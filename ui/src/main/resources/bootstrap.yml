spring:
  application:
    name: ui
eureka:
  instance:
    non-secure-port: ${server.port:8080}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/